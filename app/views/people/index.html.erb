<p id="notice"><%= notice %></p>

<center>
<h1>People</h1>
</center>

<%= link_to "Sign Out", destroy_admin_session_path %>
<div class="navbar">
  <div class="navbar-items">
    <h2>Navbar</h2>
    <div class="navbarLinkClass">
      <ul class="navbarLink">
        <li class="navbarLink" id="link1"><%= link_to('Players', players_path) %></li>
        <li class="navbarLink" id="link2"><%= link_to('Alumnis', alumnis_path	) %></li>
        <li class="navbarLink" id="link1"><%= link_to 'Events', events_path %>Create a new Event</li>
      </ul>
    </div>
  </div>
</div>

<center>
<div class="container"><table class="table table-bordered table-hover table-stripped">
  <thead>
    <tr>
      <th>UIN</th>
      <th>Name</th>
      <th>Email</th>
      <th>Phone Number</th>
      <th>Address</th>
      <th>Person Type</th>
      <th>Dues</th>
      <th>Status</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @people.each do |person| %>
      <tr>
        <td><%= person.uin %></td>
        <td><%= person.name %></td>
        <td><%= person.email %></td>
        <td><%= person.phone_number %></td>
        <td><%= person.address %></td>
        <td><%= person.person_type %></td>
        <td><%= link_to 'Show', person %></td>
        <td><%= link_to 'Edit', edit_person_path(person) %></td>
        <td><%= link_to 'Delete', delete_person_path(person)%></td>
      </tr>
    <% end %>
  
  </tbody>
</table></div>
</center>
<%= link_to 'New Person', new_person_path, :class => "btn btn-primary" %>
