<%= form_with(model: rugbyhelp) do |form| %>
  <% if rugbyhelp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rugbyhelp.errors.count, "error") %> prohibited this rugbyhelp from being saved:</h2>

      <ul>
        <% rugbyhelp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
